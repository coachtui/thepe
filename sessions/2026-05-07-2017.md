# Session: 2026-05-07 20:17 HST

## Goal
Improve the submittal register review panel summary and readiness indicators without schema changes, re-extraction, or new dependencies.

## Files Modified

### `src/components/submittal/SubmittalRegisterReview.tsx`
Two targeted changes:

**`RunSummary` component — full replacement**
- Added client-side computation of review stats from `run.items`:
  - `reviewedItems`: items where `reviewStatus` is set and not `'pending'`
  - `pendingItems`: total minus reviewed
  - `approvalRequiredItems`: items where `approvalRequired === true`
  - `lowConfidenceItems`: items where `sourceQuality === 'low'`
  - `reviewedPct`: reviewed / total × 100 (rounded)
- Expanded stat grid from 4 to 8 stats (two rows of 4):
  - Row 1: Submittal Items, Spec Sections, Ungrouped Items, Review Confidence
  - Row 2: Reviewed, Pending Review, Approval Required, Low Confidence
- Added review progress bar (CSS-only, no library) showing reviewed %
- Added contextual readiness status message:
  - 0 reviewed → "N items across S sections. Review pending items before export."
  - Partial → "X of N items reviewed (P%). M pending."
  - All reviewed → "All N items reviewed. Ready for export." (green text)
  - High item count (>1000) → appends "High item count — review by section before export."
- Improved label names: "Items" → "Submittal Items", "Sections" → "Spec Sections", "Ungrouped" → "Ungrouped Items", "Avg confidence" → "Review Confidence"
- Improved timestamp format (locale short month/day/year + time)
- Run ID display: `<span class="font-mono">` wrapper for the shortened ID

**`SectionCard` component — section-level review progress**
- Added computation of per-section reviewed/pending from `section.items`
- Updated header meta line: "N items · X/Y reviewed · avg P%"
- Added thin progress bar (1px, blue) under each section header showing section review %

## Test Results
- `npm run router:harness`: **12/12 PASS**
- `npm run build`: **Clean — no TypeScript errors, no lint errors**

## Decisions
- Computed review stats client-side from `run.items` (flat list) — no API change needed
- Used CSS-only progress bar (h-1.5 / h-1 with `style={{ width }}`) — no chart or animation library
- Status message logic: `all reviewed` → green; `none reviewed` → gray; `partial` → gray; added high-item-count suffix
- `Low Confidence` stat shows `—` when zero (not "0 flagged") to reduce visual noise

## Unresolved Items
- None for this session. The ungrouped items card header doesn't get a progress bar — could be added in a follow-up if needed.
